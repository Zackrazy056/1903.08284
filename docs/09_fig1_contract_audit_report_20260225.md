# Fig.1 审查报告（2026-02-25）

## 1. 审查目标

对照论文 `arXiv:1903.08284v2` 的 Fig.1 生成合同，检查当前实现是否满足以下关键条件：

- 峰值时间与扫描区间：`t_peak` 定义、`t0` 扫描范围、积分上限 `T`
- 频率合同：`omega_22n(Mf, chif)` 的单位与归一化
- 拟合合同：复数、时域、不加权线性最小二乘
- mismatch 合同：内积共轭定义与数值积分离散

## 2. 审查对象与配置

- 数据：`SXS:BBH:0305v2.0/Lev6`（离线缓存）
- 固定 remnant：`Mf = 0.952032939704`，`chif = 0.692085186818`
- 扫描：`t0 - t_peak in [-25M, 60M]`，步长 `1M`（共 `86` 点）
- 积分区间：`[t0, T]`，`T = t_peak + 90M`
- 拟合：complex unweighted LS（paper-like，无额外稳定性守卫）

结构化审计数据见：
- [fig1_contract_audit_20260225.json](C:\Users\97747\GW QNM EMRI PROJECT\1903.08284 overtones\results\fig1_contract_audit_20260225.json)

## 3. 审查结果

### 3.1 Step 1：时间窗合同

- `t_peak_original = 3692.6480234158453 M`
- 峰值对齐后：`aligned_peak_time = 0.0 M`
- `t0` 网格：`start=-25.0, end=60.0, n=86`
- `t0=0 -> T=90` 窗口样本数：`901`
- 采样间隔：`dt_mean = 0.0999761446 M`，`dt_std = 9.999e-06 M`

结论：通过。

### 3.2 Step 2：频率合同

`n=0..7` 的 `omega` 使用了 `qnm` 返回的 `Mf*omega` 再除以 `Mf`，单位为 `1/M`。  
关键数值（`Re(omega), -Im(omega)`）：

- `n=0`: `0.555781911`, `0.085172346`
- `n=1`: `0.543516389`, `0.257524384`
- `n=2`: `0.520795110`, `0.434818951`
- `n=3`: `0.490458593`, `0.616486168`
- `n=4`: `0.457955166`, `0.795830244`
- `n=5`: `0.439911394`, `0.970198112`
- `n=6`: `0.439635425`, `1.158095364`
- `n=7`: `0.441047571`, `1.360678996`

结论：通过。

### 3.3 Step 3：最小二乘合同（`t0=t_peak`, `T=t_peak+90M`）

相对残差 `||AC-h||2/||h||2` 随 `N` 单调下降，秩正常：

- `N=0`: `rank=1`, `cond=1.000e+00`, `rel_res=5.804e-01`
- `N=1`: `rank=2`, `cond=4.342e+00`, `rel_res=2.088e-01`
- `N=2`: `rank=3`, `cond=2.186e+01`, `rel_res=6.764e-02`
- `N=3`: `rank=4`, `cond=1.129e+02`, `rel_res=2.201e-02`
- `N=4`: `rank=5`, `cond=6.030e+02`, `rel_res=7.273e-03`
- `N=5`: `rank=6`, `cond=3.397e+03`, `rel_res=2.480e-03`
- `N=6`: `rank=7`, `cond=1.889e+04`, `rel_res=1.257e-03`
- `N=7`: `rank=8`, `cond=9.817e+04`, `rel_res=1.145e-03`

结论：通过。  
备注：`cond(A)` 随 `N` 上升明显，属于高阶 overtone 的预期数值病态趋势。

### 3.4 Step 4：mismatch 积分交叉验证

同一 `(t0,N)` 下，`trapezoid` 与 `left-Riemann` 对比：

- `N=0`: `1.840310841e-01` vs `1.856651915e-01`，差 `1.634e-03`
- `N=3`: `2.364284237e-04` vs `2.423452277e-04`，差 `5.917e-06`
- `N=7`: `6.574337198e-07` vs `6.550953817e-07`，差 `2.338e-09`

结论：通过。  
备注：高匹配区（`N=7`）积分实现差异远小于曲线结构量级。

## 4. Fig.1 现象一致性检查（paper-like）

`N=0..7` 均有 `86/86` 有效点。最优点随 `N` 增加向更早时间移动，最优 mismatch 下降：

- `N=0`: `best_t0=30M`, `best_mm=4.165e-05`
- `N=1`: `best_t0=17M`, `best_mm=4.265e-06`
- `N=2`: `best_t0=11M`, `best_mm=1.580e-06`
- `N=3`: `best_t0=7M`, `best_mm=1.128e-06`
- `N=4`: `best_t0=5M`, `best_mm=9.166e-07`
- `N=5`: `best_t0=3M`, `best_mm=7.982e-07`
- `N=6`: `best_t0=1M`, `best_mm=7.059e-07`
- `N=7`: `best_t0=0M`, `best_mm=6.574e-07`

晚时刻回升（turnover）存在且明显：`mm(t0=60M)/best_mm` 从 `~3.17e1`（`N=0`）到 `~1.21e3`（`N=7`）。

结论：与论文 Fig.1 的核心叙述一致。

## 5. 最终结论

- 当前 Fig.1 复现在方法合同层面通过审查。
- 当前观察到的“晚时刻曲线回升并靠拢”是论文中已讨论现象，不构成 bug。
- 现阶段可进入 Fig.2/Fig.4 的链路审查；建议复用同样合同框架，减少后续参数推断阶段埋雷风险。
